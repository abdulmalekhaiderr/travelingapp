import 'package:flutter/material.dart';

class FavoritesScrean extends StatelessWidget {
  const FavoritesScrean({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("هذة صفحة المفضلة "),
    );
  }
}
